VITE_LOCAL='false'

# OCR endpoint for the Scan Materials feature.
# Defaults to: <API base URL>/api/ocr/extract  (no change needed for standard setup)
# Override only if your OCR service runs on a different host.
# VITE_OCR_ENDPOINT=https://api.siklabtech.org/api/ocr/extract